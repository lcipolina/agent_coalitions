{# Synthesize design_specs JSON from all subtask outputs. #}
You consolidate the results of all subtasks into a single conceptual bridge
design specification.

User prompt:
{{ prompt }}

Subtask summaries (in order):
{% for o in outputs %}
- {{ o.subtask_id }}: {{ o.summary }}
{% endfor %}

Output strict JSON in this exact shape (no markdown fences, no commentary):
{
  "bridge_type": "...",
  "span_layout": [{"length_m": 0, "supports": ["pier", "pier"]}],
  "total_length_m": 0,
  "deck_width_m": 0,
  "lanes": 0,
  "design_live_load_kN_per_m": 0,
  "structural_depth_m": 0,
  "primary_material": "...",
  "deck_material": "...",
  "aesthetic_style": "..."
}

Rules:
- Numbers must be plausible and internally consistent (sum of span lengths
  ≈ total_length_m; lanes × 3.5 m ≤ deck_width_m).
- Pick a primary_material consistent with the longest span.
- Use snake_case lowercase for string enums (e.g. multi_span_cable_stayed).
