{# Per-subtask LLM judge: clarity / completeness / consistency 0-10. #}
You are an evaluator. Score the following subtask summary on three axes,
each on an integer scale from 0 (worst) to 10 (best):
- clarity: is it well-written and unambiguous?
- completeness: does it cover the required capabilities?
- consistency: are numbers and claims internally coherent?

Return STRICT JSON only. No markdown fences. No prose before or after.
The exact shape (all keys required, all integers must be 0-10). The numbers
below are PLACEHOLDERS \u2014 do not copy them; assign each axis its own honest
integer based on the summary, and use the full 0-10 range when warranted:
{"subtask_id": "{{ subtask_id }}", "clarity": <int 0-10>, "completeness": <int 0-10>, "consistency": <int 0-10>, "rationale": "one short sentence"}

Scoring guidance:
- 0-3: severely deficient on this axis.
- 4-6: acceptable with notable gaps.
- 7-8: solid, minor improvements possible.
- 9-10: exemplary; reserve 10 for truly outstanding output.
Do not default to 7 \u2014 differentiate based on what you actually read.

Summary to evaluate:
{{ summary }}
