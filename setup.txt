cd /Users/lucia/Desktop/Hackathon_MongoDB
git init
 create a file named exactly .gitignore (with the leading dot, no extension) at the root of your repo.

# 1. Tell git who you are (only needed once per machine)
git config user.name  "Lucia"
git config user.email "you@example.com"

# 2. A minimal .gitignore so secrets and junk never get committed
cat > .gitignore <<'EOF'
.env
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/
.DS_Store
_docx_extract/
EOF

# 3. Snapshot the spec as commit #1, BEFORE the agent touches anything
git add -A
git commit -m "spec: MVP_DESIGN.md + GAME_THEORY_PRIMER.md locked"

git reset --hard HEAD          # throw away uncommitted changes
git log --oneline              # see commits
git reset --hard <commit-sha>  # rewind to a known-good commit